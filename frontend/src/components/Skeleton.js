export default function Skeleton() {
    return (
        <div
            style={{
                background: "rgba(255,255,255,0.05)",
                height: "100px",
                borderRadius: "12px",
                animation: "pulse 1.5s infinite"
            }}
        />
    );
}